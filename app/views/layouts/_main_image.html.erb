<% content_for :extra_js do %>
	Utils.nextSection();
<% end %>

<section class="<%= main_image_class %>" style="background-image:url('<%= main_image %>');">
	<% if params['action'] == 'home' %>
		<%= image_tag 'logo-finca-ferrer.png' %>
		<%= link_to '#', class: 'NextSection' do %>
			<%= image_tag 'arrow-down.png', class: 'arrow-down' %>
		<% end %>
	<% end %>
</section>